import { invoke } from "@tauri-apps/api/core";
import type { Account } from "./types";

export type Api = { status(): Promise<Account>; createAccount(): Promise<void>; importAccount(backup: string): Promise<void>; selectAccount(id: string): Promise<void>; configureRelay(server: string): Promise<void>; migrateRelay(server: string): Promise<void>; send(to: string, text: string): Promise<void>; groupSend(group: string, text: string): Promise<void>; importContact(card: string): Promise<void>; shareCard(): Promise<string>; markRead(conversation: string): Promise<void> };
export const api: Api = { status: () => invoke("account_status"), createAccount: () => invoke("create_account"), importAccount: backup => invoke("import_account", { backup }), selectAccount: id => invoke("select_account", { id }), configureRelay: server => invoke("configure_relay", { server }), migrateRelay: server => invoke("migrate_relay", { server }), send: (to, text) => invoke("send_direct", { to, text }), groupSend: (group, text) => invoke("send_group", { group, text }), importContact: card => invoke("import_contact", { card }), shareCard: () => invoke("share_card"), markRead: conversation => invoke("mark_read", { conversation }) };
